import { createBrowserRouter } from 'react-router-dom';

const router = createBrowserRouter([
  {
    path: '/',
    element: <h1 className="bg-rose-600 text-white">Hello world!</h1>,
  },
]);

export default router;
